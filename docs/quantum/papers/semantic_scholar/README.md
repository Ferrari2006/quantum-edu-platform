# Semantic Scholar Metadata

Use this directory for Semantic Scholar paper metadata, citation context, and related-paper recommendations.

Keep API response-derived metadata separate from internally written paper summaries.

First-batch policy:

- Store metadata, abstracts when permitted, citations, recommendations, and URLs.
- Do not download or locally store full-text PDFs by default.
